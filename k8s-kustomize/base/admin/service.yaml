apiVersion: v1
kind: Service

# 기본 정보
metadata:
  name: admin-service

# 스펙
spec:
  type: ClusterIP

  # 라벨 셀렉터
  selector:
    app: admin

  # 포트 및 타겟 포트
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
