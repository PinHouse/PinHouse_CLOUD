apiVersion: apps/v1
kind: Deployment

# 기본 설정
metadata:
  name: pinhouse-be

# 스펙
spec:
  replicas: 1

  template:
    metadata:
      labels:
        environment: dev

    spec:
      containers:
        - name: pinhouse-be
