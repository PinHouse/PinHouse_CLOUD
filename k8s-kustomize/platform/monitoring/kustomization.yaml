# ========================================
# Grafana HTTPRoute (NGINX Gateway Fabric)
# ========================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - httproute.yaml
