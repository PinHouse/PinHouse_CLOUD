apiVersion: v2
name: pinhouse-platform

# 설명
description: PinHouse Platform Infrastructure Helm Chart (cert-manager, nginx-gateway, network-policy)

# 타입
type: application

# 헬름차트 버전
version: 1.0.0

# 애플리케이션 버전
appVersion: "1.0.0"
